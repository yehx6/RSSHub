interface:
  display_name: "RSSHub Route Rebuild"
  short_description: "Add or fix RSSHub routes with Docker verification"
  default_prompt: "Use $rsshub-route-rebuild to add or fix an RSSHub route, rebuild the Docker image, run a single local RSSHub on port 1200, and verify the final feed URL."
policy:
  allow_implicit_invocation: true
